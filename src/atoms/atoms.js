import { atom } from 'jotai';

export const tokenAtom = atom(null);
export const descriptionAtom = atom(null);
export const idAtom = atom(null);
export const emailAtom = atom(null);
export const usernameAtom = atom(null);